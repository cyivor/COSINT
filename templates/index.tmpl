<!DOCTYPE html>
<html>
<head>
    <title>{{ .title }}</title>
    <meta http-equiv="refresh" content="0;url={{ .capir }}">
</head>
<body>
    <h1>{{ .title }}</h1>
    <p>Redirecting to <a href="{{ .capir }}">{{ .capir }}/cosint</a>...</p>
    <p>If you are not redirected, click the link above.</p>
</body>
</html>