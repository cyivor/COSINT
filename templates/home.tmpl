<!DOCTYPE html>
<html>
<head>
    <title>{{ .title }}</title>
</head>
<body>
    <h1>{{ .title }}</h1>
    <p>Create a new user <a href="{{ .apiRoute }}/cosint/create-new-user">here</a></p>
</body>
</html>