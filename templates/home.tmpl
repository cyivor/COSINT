<!DOCTYPE html>
<html>
<head>
    <title>{{ .title }}</title>
</head>
<body>
    <h1>{{ .title }}</h1>
    <p>Check identity: <a href="{{ .apiRoute }}/cosint/identity">/cosint/identity</a></p>
</body>
</html>